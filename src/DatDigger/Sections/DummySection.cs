namespace DatDigger.Sections
{
    /// <summary>Used for unimplemented sections so the section loader doesn't barf when it finds an unknown section type</summary>
    public class DummySection : SectionBase
    {
    }
}
